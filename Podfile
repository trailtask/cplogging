# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'CPLogging-iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CustomLogging-iOS

  target 'CPLogging-iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'CPLogging-macOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CustomLogging-macOS

  target 'CPLogging-macOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
